# Never commit real secrets or local junk.
.env
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# Database backups written by the db-backup service / `make backup`.
backups/
.pytest_cache/

# Vendored exercise demo images (~99 MB). Not committed -- run
# `python backend/scripts/vendor_exercise_images.py <free-exercise-db checkout>`
# once per fresh checkout, before `docker compose build`.
backend/static/exercises/

# macOS
.DS_Store
.AppleDouble

# Windows
Thumbs.db
desktop.ini

# Editors & IDEs
.vscode/
.idea/