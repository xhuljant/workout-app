# Alembic configuration.
#
# The database URL is NOT set here on purpose -- alembic/env.py reads it from the
# app's own settings (the DATABASE_URL environment variable), so migrations and
# the app can never disagree about which database they mean.

[alembic]
script_location = alembic
prepend_sys_path = .
# Revision filenames: 0001_slug, 0002_slug, ... (zero-padded, sortable).
file_template = %%(rev)s_%%(slug)s
version_path_separator = os

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
