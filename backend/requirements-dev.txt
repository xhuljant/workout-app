# Extra dependencies for running the test suite (backend/tests/). Not installed
# into the production image. Used by CI and by `make test`.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.3.4
httpx==0.28.1              # FastAPI's TestClient transport
